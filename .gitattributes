# Keep the patches as they were written. This is hygiene rather than the thing
# that makes them apply -- a Windows runner handed them over as CRLF regardless
# of this, which is why the build applies them with --ignore-whitespace.
*.patch text eol=lf

# Input scripts are parsed line by line and read the same way everywhere.
tests/scripts/*.txt text eol=lf

*.png binary
